# 全民求生・系统搜打撤

> 末世废土 · 文字放置 ·「搜—打—撤」硬核生存
>
> 当前版本：**v1.1.9** ｜ 纯前端 `localStorage` 存档，开箱即玩，无需后端

当文明崩塌，废土成为唯一的世界。一个名为「系统」的存在向每一位幸存者发布指令：**进入危险区域，搜刮物资，击退威胁，活着撤出来。**

《全民求生・系统搜打撤》是一款数据驱动的文字放置生存游戏。你经营一支幸存者战团，在废弃都市、军事禁区与核爆禁区之间反复出击，用每一次「搜打撤」积累装备、货币与声望，在死亡与撤离的张力中撑到明天。

---

## 游戏概述

| 维度 | 说明 |
| --- | --- |
| 题材 | 末世废土 + LitRPG「系统」包装 |
| 主循环 | 系统发布目标 → **搜**（搜刮）→ **打**（战斗）→ **撤**（撤离结算） |
| 爽点 | 高风险高回报的撤离、装备积累、战团养成、据点经营 |
| 呈现 | 纯文字 + 数据面板，无美术资源依赖，桌面/移动端浏览器均可游玩 |

---

## 核心玩法大纲

### 1. 搜打撤主循环

```
        系统发布目标
             │
             ▼
    ┌─────────────────┐
    │   据点（安全区）   │
    └────────┬────────┘
             │  出击
             ▼
    ┌───────────────────────────┐
    │      危险区域（副本）        │
    │   搜：搜刮建筑 / 容器 / 尸体  │
    │   打：遭遇战（丧尸 / 掠夺者） │
    │   撤：抵达撤离点结算          │
    └────┬───────────────┬──────┘
  撤离成功 │               │ 阵亡 / 撤离失败
         ▼               ▼
  ┌────────────┐   ┌──────────────────┐
  │ 物资入库    │   │ 未撤离物资全部遗失  │
  └─────┬──────┘   │ 出击者进入濒死状态  │
        │          └──────────────────┘
        ▼
   休整（恢复 HP / 治疗伤势）→ 强化装备 → 再出击
```

- **搜（Loot）**：进入危险区域探索建筑与容器，搜刮材料、弹药、食物与装备；越危险的区域产出越好。
- **打（Fight）**：遭遇战由真实战斗引擎结算（技能、暴击、增益、伤势全生效），敌人涵盖丧尸群、变种生物、掠夺者小队与区域首领。
- **撤（Extract）**：抵达撤离点即结算。**成功 → 本局搜刮入库；失败/阵亡 → 仅丢失本局未撤离物资**，据点资产与已装备安全。

### 1.1 成长与升级（实时同步）

经验与等级在**角色页与出击页共享同一份档案**，全程实时同步：

- **出击实时获取经验**：每次击杀/战斗后立即结算经验并写入角色档案，无需等到撤离结束；
- **途中升级即时生效**：出击过程中经验满级即升级，**状态回复全满 + 伤势清除**，并弹出**词条三选一**提示，选定后立刻生效；
- **加点 / 选词条两边通用**：能力加点（每级 +3 自由属性点）与升级词条三选一，既可在**角色页**操作，也可在**出击页**操作，两边实时同步、互不冲突；
- **词条属性计入基础六维**：升级获得的词条六维加成在选取时即并入角色基础属性，显示为白色基础值（不再以绿色「加成」重复呈现），未穿戴装备时不会凭空出现绿字。

### 1.2 出击换装规则

- 出击途中可在临时背包里就地「🎽 佩戴 / 卸下」装备；
- **带「增加气血」的装备：佩戴后只提升最大血量、脱下只降低最大血量，不影响当前血量**（满血时减最大则当前同步减少，满血加最大则当前不变，非满血时当前血量不受影响）；
- 而**加体质点**则是最大血量与当前血量**一起提升**；
- 撤离（成功）后，出击过程中换上的装备（含副本内搜到的掉落装备）会**保留并同步回角色装备栏**，后续出击沿用。

### 2. 战团与幸存者

- **主角生成**：建立档案时，以玩家代号生成一名**全属性 15、紫色（epic）品质**的主角，直接入列战团。
- **战团成员（N / 10）**：战团上限 **10 人**。副本中带回的幸存者不会自动入团。
- **幸存者花名册**：展示副本中发现的幸存者，含**属性面板 + 招募金额**（越强越贵，按下表分档）：

  | 段位 | 1 | 2 | 3 | 4 | 5 |
  | --- | --- | --- | --- | --- | --- |
  | 招募费（废土币） | 50 | 200 | 800 | 3000 | 10000 |

- **招募 / 遣散**：花废土币招募后正式入列；战团满员无法招募，需**遣散**成员（返还 **1/3** 招募价值）或等成员死亡腾位。**主角不可遣散**。

### 3. 濒死与救治

撤离失败 / 阵亡的成员返回战团会进入**濒死**状态：

- 血量骤降至约 **5%**，并附带流血 / 骨折 / 惊惧等伤势，期间不可再次出击；
- 开启 **10 分钟救治倒计时**；
- 可用 **200 废土币**或任意**医疗品**救治（恢复约 50% 血量并清除伤势）；
- 若**长时间未救治**，成员死亡并自动离开战团。

### 4. 装备与制造

- 战利品含带**阶级词缀**的装备（锈蚀 → 改制 → 精工 → 军用 → 霸主 → 超凡 → 神话，对应白→红七阶），阶级越高属性越强、出现越稀有；
- 制造 / 改装台消耗材料与废土币产出带词缀的武器、护甲、义体、配件；
- 装备的战斗词条（气血上限 / 暴击 / 搜刮运势）真实带入出击。

### 5. 避难所与据点

- 避难所设施（瞭望塔 / 医疗站 / 改装工坊 / 训练场）可升级，提供驻防被动；
- 战团声望投资解锁长期加成；
- 据点温室 / 生产车间提供稳定产出。

### 6. 末世行止（菜单系统）

仿系统终端的菜单，涵盖：医疗中心、废土市场、探险札记、任务中心、英雄榜、世界传闻、兑换码、意见反馈、系统设置等模块。

---

## 技术概览

- 服务端：`Hono` + `Bun`
- 前端：`React 19` + `React Router 7` + `Vite`
- 样式：`Tailwind CSS 4`
- 战斗引擎：时间轴回合制战斗（技能 / 词条 / Buff 全配置驱动）
- 持久化：浏览器 `localStorage` 存档（按账号隔离），无需服务端数据库即可游玩
- AI 旁白：可接入 LLM 做「末世生存系统」实时播报

## 目录结构（核心玩法）

```text
.
├── src/shared/engine/survival/   # 生存模式引擎
│   ├── chargen.ts                # 幸存者/主角随机生成（六维 + 词条 + 段位 + 稀有度）
│   ├── state.ts                  # 持久化状态与操作（招募/遣散/入库/制造/出击装配）
│   ├── recovery.ts               # HP / 伤势 / 濒死 / 撤离后恢复
│   ├── economy.ts                # 废土币、制造改装、避难所、战团声望
│   ├── extraction/               # 出击-搜刮-战斗-撤离 状态机
│   └── combatAdapter.ts          # 幸存者 / 敌人接入战斗引擎
├── src/react-app/routes/survival/
│   ├── route.tsx                 # 落地页（/survival）
│   ├── signup/route.tsx          # 建立幸存者档案（/survival/signup）
│   ├── login/route.tsx           # 幸存者核验（/survival/login）
│   ├── play/route.tsx            # 主玩法 Hub：战团/花名册/出击/医疗（/survival/play）
│   └── menu/                     # 末世行止菜单（/survival/menu）
└── src/shared/theme/survival.ts  # 主题层：名词映射与「系统」旁白文案
```

## 本地运行

环境要求：安装 [Bun](https://bun.sh)（≥ 1.x）。

```bash
bun install        # 安装依赖
bun run dev        # 启动开发服务器（前端 + API 并行）
# 浏览器打开 http://localhost:5173/ ，进入 /survival 注册即可游玩
```

其他命令：

```bash
bun run build      # 生产构建
bun run lint       # ESLint
bun run check      # 类型检查
bun run test       # 运行 src/shared 下的纯引擎单测
```

## 版本更新日志

### v1.1.9（2026-09-13 · 已发布）

> 末世赌局 + 废土拍卖行实装，漫游文案与任务中心扩容，并修复漫游掉血/赌局掉状态/背包重复显示等体验问题。

**① 末世赌局（实装 + 切出保留）**
- 末世行止新增「末世赌局」：随机生成两名战力相近的临时幸存者 1v1，玩家押注后点「开始切磋」，战斗 UI 参考擂台切磋、逐回合点「下一回合」。
- 修复切出菜单再切回会刷新丢失已投注未结束赌局的问题：进行中赌局完整持久化进存档（含 mid-duel 战斗存档 `BattleSaveV1` 与 `initialTimeline`），切回时 `restoreArenaDuelSession` 续打，本金不再白扣。

**② 废土拍卖行（实装）**
- 装备库 / 末世行止新增「拍卖行」：废土游商随机上架 **10 件**装备（危险度梯度分布，低中高阶级皆有），花废土币购买入背包。
- 价格随阶级平滑上浮：**锈蚀约 200 / 改制约 430 / 精工约 930 / 军用约 2000 / 霸主约 4310 / 超凡约 9270 / 神话约 20000**（±8% 抖动）。
- **30 分钟倒计时自动刷新**；也可花 **500 废土币立即再刷一轮**（与自动刷新独立，不重置倒计时）。
- 卡片按装备阶级 `tierColor` 着色（边框 / 名称 / 稀有度徽标 / 词条 chip），六维与战斗加成分色呈现。

**③ 漫游文案与任务中心**
- 漫游搜打撤出动文案「派出一支小队」→「派{xx}出击」，取消冗余「· 出击者：xx」行。
- 任务中心每日任务上限由默认扩展至 **10 个**。

**④ 装备价值体系统一**
- 装备「显示价值」直接等于「售出价值」（新公式 `gearBaseValue`：基础 `(40 + 阶级×45 + 词条数×25) × (1 + 阶级×0.2)`）。
- 旧存档读档自动按 `gearBaseValue(tier, 词边数)` 重算 `value`，无需刷新即可显示正确价值；背包 / 改装页估值统一走 `gearSellPrice` 实时重算。
- 各阶级价值区间（显示 = 售出）：锈蚀 65~115 / 改制 132~192 / 精工 186~287 / 军用 320~400 / 霸主 441~531 / 超凡 580~680 / 神话 737~847。

**⑤ 体验修复**
- 漫游搜打撤「有伤势却无掉血」不一致：震伤改为仅在血量受损（<100%）时触发；感染生效时额外造成 5% 最大生命的侵蚀伤害。
- 背包装备库重复显示属性（如「生命值+60、生命值+225」）修复：统一复用 `GearBonusChips`，六维灰底、战斗加成 tierColor 底，同一属性只出现一次。

### v1.1.8（2026-09-13 · 已发布）

> 废土市场出售体验打磨 + 夹板绷带配方调整 + 升级天赋三选一权重重平衡。

**① 医疗品单价 ×2（含制作配方手续费）+ 菜园文案精简**
- `state.ts`：`MEDICINES` 8 项单价全部 ×2（绷带 15→30 / 抗生素 25→50 / 急救箱 60→120 / 肾上腺素 40→80 / 营养剂 35→70 / 血清 70→140 / 纳米凝胶 120→240 / 夹板绷带 50→100）；`MED_CRAFT_RECIPES` 6 项手续费同步 ×2（绷带 5→10 / 肾上腺素 12→24 / 营养剂 8→16 / 血清 18→36 / 纳米凝胶 35→70 / 夹板绷带 25→50），制作台出击临时制作台 `sortieNeeds` 一致。
- 避难所·菜园：副标题取消「（成熟收获后回本并盈利）」；种子商店取消「净赚 +N 币」文案，仅保留真实市价对照。

**② 废土市场·出售物资交互优化**
- 材料「全部」出售改为**内联二次确认**（显示数量 + 所得废土币，确认/取消），不再一键清空整类材料。
- 仓库装备新增**按阶级一键批量出售**：`一键出售锈蚀阶（n）/ 改制阶（n）/ 精工阶（n）/ 军用阶（n）`（按 `tier` 0~3 聚合，空阶禁用），点击弹内联二次确认后整阶卖出并同步移除已选。
- 仓库装备列表改为**按阶级升序排列**（锈蚀在上、神话在下），已选/总价计算不受影响。

**③ 医疗·制作台：夹板绷带合成材料调整**
- `craft-splint` 由「废金属 2 + 化工制剂 1 + 100 币」改为 **「废金属 2 + 50 币」**，不再需要化学制剂；出击临时制作台 `sortieNeeds` 同步为 `scrap×2`。

**④ 升级天赋三选一抽取权重重平衡**
- `chargen.ts: TRAIT_PICK_WEIGHT`：白·锈蚀 2.0→**2.8**；紫→1.0 / 黄→0.8 / 橙→0.6 / 红→0.4（各降 0.2）；绿、蓝不变。三选一展出中至少出现 1 个该阶级的实现概率：白 82.7% / 绿 55.8% / 蓝 40.5% / 紫 27.0% / 黄 13.6% / 橙 7.0% / 红 2.4%。
- 修正 `chargen.ts:976` 误导性注释：原注释「白16/绿12/蓝10/紫10/黄6/橙4/红2 共 60 个」实为该词条**池内数量构成**，被误当成权重；现明确标注「这串数字只是抽取权重，与池内数量无关」。

### v1.1.7（2026-09-12 · 已发布）

> 副本体验与末世行止功能大整修：漫游平衡、肾上腺素重做、感染 debuff 实装、救治交互调整。

**① 漫游搜打撤平衡修复（0 伤害 / 收益过高）**
- 恢复「危险度越高敌人越强」梯度 `ENEMY_DANGER_SCALE`：危1 1.0 → 危7 1.6（每级约 +10% 基础六维，不造成数值悬崖）。
- 区域选择向高危倾斜，收益折扣 0.5，AI 遇敌即正面开战并搜尸；首搜强制遭遇，确保不再「无伤白嫖」。
- 模拟验证：真实主角（全15）0 无伤、掉血真实、阵亡率 9%；弱角高风险对应其自身属性，符合「托管不如手动划算」。

**② 装备系统改造**
- 背包页移除「装备制造」UI；末世行止「装备改装」改为：选择当前出击者已装备，花费 500 币重 roll 同阶级词条，5% 概率升 1 阶。
- 装备卡片不再显示角色段位徽标（段位仅属角色战力区间）。

**③ 肾上腺素重做与文案统一**
- 取消回血/消疲惫，仅保留「副本时间 10 分钟内六维全属性 +5」的计时增益。
- 修复临时背包使用肾上腺素无增益的 bug；医疗中心移除肾上腺素治疗选项；医疗·制作台/快捷槽/废土市场文案统一为「增益补给，副本时间 10 分钟内六维全属性 +5」。

**④ 战斗与回放**
- 修复擂台切磋闪避日志「自己闪自己」的文案错误。
- 战斗回放六维点评去掉「灵性」，改为正确维度「感知」。

**⑤ 感染 debuff 实装**
- 深3~深7 区域，搜刮 / 战斗 / 移动均有 5% 概率感染；感染削减体质/意志并降低恢复速度，可用抗生素 / 血清 / 纳米凝胶清除。

**⑥ 副本药物与救治**
- 副本药物产出概率整体降至 50%（搜索 / 搜尸均经同一落点）。
- 濒死救治费用 100 → 200 废土币（实际扣费同步）。
- 角色页濒死卡片新增「纳米凝胶救治」；医疗中心新增「一键救治（当前出击者 · 500 币）」：生命全满并清空全部 debuff / 濒死。

### v1.1.6（2026-09-12 · 已发布）

> 擂台切磋系统（本地 1v1 模拟对决，不联网）+ 装备废土风重命名（含旧存档读档迁移）。

**① 擂台切磋系统（本地 1v1 模拟对决，不联网）**
- 「末世行止」新增**擂台切磋**：选两位战团成员，双方**满血开局**，逐回合点「下一回合」推进交手；实时显示双方六维 + 战斗属性与掉血 / 闪避 / 阵亡日志，一方阵亡即结束。
- 对决单位从存档**全新拷贝**、全程在独立战斗存档上跑，**结束天然恢复战前状态**（杜绝借切磋刷血）；切磋属性仅含角色六维 + 装备属性，**剔除基地 / 避难所 / 势力的各类属性 buff**（与出击 / 遇怪战斗的数值口径不同，专用于成员间公平模拟）。

**② 装备废土风命名 + 旧存档读档迁移**
- 装备品质名由「白阶 / 绿阶 / …」改为废土风：**锈蚀 → 改制 → 精工 → 军用 → 霸主 → 超凡 → 神话**（白→红对应），槽位名（主武器 / 副武器 / 头部 / 护甲 / 腿部 / 配件）保留；配色 `tierColor` 不变。
- 旧存档装备名（掉落时烘焙进存档）经 `persistence.ts: loadGame` 读档迁移，按 tier+slot **幂等重算**为新名，下次读档即生效、无需清档；搜刮 / 霸主战利品日志同步去掉「阶」字。

### v1.1.5（2026-09-11 · 已发布）

> 跨设备存档同步（零后端，两种方式任选；存档码经压缩，可直接发微信）。

**① 跨设备存档同步（两种方式）**
- `menu/views.tsx: ViewSettings`「系统设置」→「跨设备存档同步（存档码 / 存档文件）」卡片，提供两种搬进度方式，均需在目标设备登录同一账号名才生效：
  - **方式一 · 存档码（已压缩，适合微信）**
    - **生成存档码**：整份存档经 `buildTransferSnapshot` 裁剪后由 `lz-string` 压缩 base64 编码（`compressToBase64`）。裁剪范围：已装备 gear 全部保留；未装备 gear 按价值保留前 50 件；待招募按战力保留前 10 人；历史日志 log 留最近 30 条、出击记录 sortieHistory 留最近 20 条、漫游记录 wanderLog 留最近 10 条。被裁剪的装备/待招募导入后**无法恢复**，故生成前会弹二次提醒说明后果；如需完整无裁剪备份请用方式二「下载存档文件」。实测极端存档（1000 件 gear + 100 名待招募）压缩后仍约 1.2 万字符，远低于微信约 1.6 万字符上限。可直接粘贴到微信 / 邮件，配「复制存档码」一键复制。
    - **从存档码导入**：粘贴存档码 → 校验（`version` + `survivors`）→ 内联二次确认覆盖当前账号存档 → 刷新载入。兼容旧版未压缩 base64 与原始 JSON。
  - **方式二 · 存档文件（下载 / 上传）**
    - **下载存档文件**：导出 `wasteland-save-<账号>-<日期>.json` 到本机，**完整保留全部装备、待招募与日志（不做任何裁剪）**，适合做完整备份或在多设备间无损迁移。桌面端直接下载；**手机 / 微信端点击会先弹窗**，可选「① 尝试导出 .json 文件」或「② 生成完整存档文本供复制」（微信内置浏览器通常会静默拦截文件下载，此时选 ② 复制文本即可）。
    - **从存档文件导入**：选择 .json 文件 → 同样走校验 + 二次确认流程。
- **内联二次确认**（替代失效的 `window.confirm`）：
  - **生成存档码前**：弹出琥珀色提醒，说明存档码会裁剪未装备 gear（留前 50）、待招募（留前 10）与历史日志，导入后裁剪内容不可恢复，提示需要完整备份请改用下载文件；含「取消 / 确认生成」。
  - **导入前**（两种方式通用）：弹出明确提示「即将覆盖当前账号『xxx』存档，不可撤销」，含「取消 / 确认导入」。
- 原理：存档码 / 文件 = 当前账号存档；换设备登录同一账号名（本地账号，纯槽位钥匙）后导入即生效，无需服务器。

### v1.1.4（2026-09-11 · 已发布）

> 取消独立「资质/rarity」维度：幸存者生成由**唯一段位维度**驱动（属性范围 / 词条数 / 招募价格 / 显示均按段位）。初始生成覆盖段位 1~5（废土新人→旷野狂徒），段位 6~7（荒域掌控者 / 末世传奇）仅由战力成长达成。

**① 取消资质维度，生成全面段位化**
- `chargen.ts`：删除 `SurvivorRarity` / `RARITY_INFO` / `rarityColor` / `rarityLabel`，新增 `GEN_TIERS`（段位 1~5）统一驱动：
  - 六维单维区间 / 六维总和（战力）区间 / 初始词条数 / 招募费：
    - 废土新人：单维 6~14，战力 36~59，词条 1，招募 200
    - 资深拾荒者：单维 8~16，战力 60~83，词条 2，招募 800
    - 战团骨干：单维 10~18，战力 84~107，词条 2，招募 2000
    - 钢铁幸存者：单维 12~22，战力 108~131，词条 3，招募 5000
    - 旷野狂徒：单维 14~25，战力 132~150，词条 3，招募 10000
  - 生成即「所见即所付」：段位锁定为抽到的生成档，**不**因词条带来的属性增量而升级，避免「付了低价却显示高档」的错配。
  - **属性 + 词条共同生成**：基础六维按词条加权战力增量反向定位目标区间，保证「算上天赋词条后的最终战力」100% 落在段位区间内（抽样 2000×5 档验证）。
  - **初始词条品质按段位上限**：废土新人仅白/绿，资深拾荒者最高蓝，战团骨干最高紫，钢铁幸存者最高黄，旷野狂徒才可出橙/红；杜绝低段位附带高阶天赋的割裂。
- `TIERS` 段位门槛对齐新战力区间：废土新人 0 / 资深拾荒者 60 / 战团骨干 84 / 钢铁幸存者 108 / 旷野狂徒 132 / 荒域掌控者 180 / 末世传奇 240。
- `state.ts`：`recruitFee` 改为 `[200,800,2000,5000,10000]`；`recruitSurvivor` 在付得起的段位（1~5）内加权抽档并按段位付费；分配属性点 / 选词条 / 重洗 / 换装四处段位重算保留「只升不降」（`Math.max(genTier, 战力段位)`）。
- `persistence.ts`：旧档迁移收紧——仅当 `tier` 缺失或 `tierName` 非法时才按战力重算，保留生成锁定的段位与已正确的旧档。
- `views.tsx` / `route.tsx`：删去花名册与副本内招募卡的「资质·X」标签，姓名/边框配色改由段位色驱动；现仅显示一个段位徽标，不再割裂。
- `menu/views.tsx: GmPanel`：GM 解锁后新增「随机生成幸存者」琥珀色按钮，按 GEN_TIERS 权重随机生成并加入花名册，方便调试验证。
- 旧存档兼容：移除的 `rarity` 字段不参与存档结构（引擎层已无引用），加载时按新规则处理。

### v1.1.3（2026-09-11 · 已发布）

> 末世行止·掌握技能优化、医疗制作台与战术手册 UI 美化、材料文案正名；追加四大菜单页 UI 美化（花名册 / 战团技能 / 全部战绩 / 英雄榜）；段位扩至 7 档（白→红）并全站配色；旧存档段位迁移修复；角色页/英雄榜去重 rarity 标签。

**① 掌握技能词条按品质着色**
- 原 `ViewSkills`（末世行止·掌握技能）把所有已掌握天赋词条统一用翠绿（emerald）显示，看不出品质差异。现改用 `affixColor` / `affixLabel` 按词条品质（白/绿/蓝/紫/黄/橙/红）渲染文字、边框与半透明底色，并附小型品质标签，与装备词缀、升级三选一卡片同套配色。`menu/views.tsx: ViewSkills`

**② 花费废土币重洗单项词条（二次确认 + 三选一，不重复已掌握）**
- 每个已掌握词条旁新增「重洗」按钮 → 弹**二次确认**弹窗：明示花费 `1000` 废土币、替换后不可撤销、新词条不会与已掌握项重复；废土币不足时禁用确认并提示差额。
- 确认后从「该成员当前已拥有的全部词条（含被重洗那一条）」之外的池中抽取 **3 枚三选一**；选定其一即替换原词条（旧词条属性增量回退、新词条叠加，并重新计算战力/段位与最大生命上限），同时扣除 1000 废土币。已掌握全部则提示「无可用候选」。
- `state.ts` 新增 `rerollTrait(state, survivorId, oldTraitId, newTrait)` 与常量 `REROLL_TRAIT_COST = 1000`（复用 `chooseTraitPick` 的属性回退/重算口径）。`menu/views.tsx` / `state.ts`

**③ 医疗·制作台 UI 美化 + 材料文案正名**
- 材料类别名修正：`MATERIAL_LABEL` 中 `food` 由 `compact口粮` → `压缩口粮`、`metal` 由 `金属 scraps` → `废金属`。该映射被「医疗·制作台」「装备改装」「副本背包材料栏」等全部引用点共用，故**一处修正、全局生效**，其余出现此类中英混排文案的地方一并替换。
- 医疗·制作台（`ViewCraft`）卡片重排：标题加 ⚗️ 图标与「就地补给」标签；产出改为右上角红色 pill；材料清单改为「持有/需求（have/qty）」左右对照、足/不足双色高亮；合成按钮统一圆角与 hover 态。`menu/views.tsx: ViewCraft`

**④ 战术手册 UI 美化 + 词条按品质着色**
- 战术手册（`ViewTactics`）的「被动技 · 战斗词条」卡片，按词条 `quality` 用 `affixColor` / `affixLabel` 渲染**左侧色条 + 品质标签**（白/绿/蓝/紫/黄/橙/红），并与「掌握技能」同套着色规范；标题加 📓 图标与「按品质着色」提示，六维基础属性条保留。`menu/views.tsx: ViewTactics`
- 副本背包「材料」栏同步美化：每项材料前加按类别着色的圆点（金属/电子/化工/异变/口粮/杂项），与材料名、数量、价值同排展示。`play/route.tsx`

**⑤ 幸存者花名册 UI 美化（`ViewRecruits`）**
- 标题加 🪪 图标；卡片按「稀有度」用 `rarityColor` 着色边框与姓名；「稀有度」标签改用 `rarityLabel` 圆角 pill；战力/段位维持。
- 词条列表由统一天蓝改为**按品质用 `affixColor` 渲染**（文字色 + 半透明底色 + 同色描边 + 色点），与「掌握技能」「战术手册」同套。
- 招募费改为独立右上角展示，并按「是否可负担 / 战团是否满员」自动切换 `emerald-400` / `rose-400` 配色；「招募」按钮改为整行主按钮（招募入团）、「放走」改为次级灰色按钮。修复原「放走」按钮类名 `bg-zinc-9500`（非法 Tailwind 类，渲染无效）笔误。

**⑥ 战团技能 UI 美化（`ViewFactionSkills`）**
- 标题加 🏛️ 图标；每个势力配专属图标 + 主题色（铁壁🛡️红 / 银手💰金 / 自由侦察兵🧭蓝），卡片边框与进度条按主题色着色。
- 新增 5 格「声望进度条」（lv 0~5 点亮），并实时汇总「当前全团加成」（如 力量+3 · 体质+3），原「每级 +N」列表保留。`menu/views.tsx: FACTION_META`

**⑦ 全部战绩 UI 美化（`ViewBattleLog`）**
- 标题加 📜 图标；每条战绩改为「左侧 outcome 色条 + 内容」卡片，按结果着色（撤离成功✅绿 / 阵亡💀红 / 超时⏳金），结果标签用同色圆角徽标。
- 元数据分行排版：日期时间、入库件数（绿）、废土币（金）、遭遇敌人、救援徽标（天蓝），信息密度更高且一目了然。`menu/views.tsx: SORTIE_OUTCOME_META`

**⑧ 英雄榜 UI 美化（`ViewLeaderboard`）**
- 标题加 🏆 图标；榜首三名显示 🥇🥈🥉 奖牌（其余显示数字排名）；姓名与「稀有度」标签按 `rarityColor` 同色渲染（文字色 + 半透明底色 pill）。
- 战力列加金色高亮，新增「词条」列显示该成员已掌握词条数；表头补齐、行 hover 高亮。`menu/views.tsx: LEADERBOARD_MEDAL`

**⑨ 段位阶梯扩展至 7 档 + 段位配色 UI**
- 段位由 5 档扩至 **7 档**，颜色对应白→红（与词条品质同套色板）：
  - T1 废土新人（白 ≥0）· T2 资深拾荒者（绿 ≥55）· T3 战团骨干（蓝 ≥75）· T4 钢铁幸存者（紫 ≥95）· **T5 旷野狂徒（黄 ≥120，新增）** · **T6 荒域掌控者（橙 ≥160，新增）** · **T7 末世传奇（红 ≥200）**。
  - 用户指定：末世传奇=段位7 且战力 ≥200；T5/T6 名称如题；其余阈值顺延原梯度的 55/75/95 并补齐中段 120/160。
- `chargen.ts`：`TIERS` 常量增加 `color` 字段（白绿蓝紫黄橙红）；新增 `tierColor(tier)` 按段位取配色；`tierFromPower` 不变（仍返回 `{tier,name}`）。`theme/survival.ts: SURVIVAL_TIERS` 同步更新为 7 档名。
- UI 配色：新增 `TierBadge`（菜单页）与副本内联徽标，均按段位取色（文字+描边+底色同色系半透明）；覆盖 花名册/英雄榜/战团成员/战术手册/掌握技能/重塑六维（菜单）与 副本内角色卡/招募卡/出击者条（route.tsx）。`menu/views.tsx` / `play/route.tsx`
- 顺带修复：上轮 ⑤⑥⑦⑧ 给 `Card` 传 `style` 的依赖（当时未跑 tsc 未发现）——`Card` 补 `style?: React.CSSProperties` 支持；副本内 `tierColor` 复用 `affixes.tierColor`（0 索引，传 `tier-1`）避免与 chargen 导入重名。

**⑩ 修复旧存档段位名称/颜色不一致（5→7 档迁移）**
- 问题：旧存档按原 5 档段位写入 `tierName`（≥120 即「末世传奇」），扩到 7 档后加载会出现「战力 182 仍显示末世传奇」或「颜色与名称不匹配」。
- 修复：
  - `persistence.ts: loadGame` 增加加载迁移：所有 `survivors` / `recruits` 按当前 `power` 重新调用 `tierFromPower` 写入 `tier` 与 `tierName`。
  - `chargen.ts` 导出 `TIERS` 并新增 `tierNameFromTier(tier)`；`menu/views.tsx: TierBadge` 改为从 `tier` 推导名称（不再信任可能 stale 的 `tierName`），确保徽标颜色与名称永远一致。
  - `play/route.tsx` 副本内三处 inline 段位徽标文本也由 `tierNameFromTier(s.tier)`/`r.tier`/`active.tier` 推导，避免 stale 文本。
- 结果：战力 182 → T6「荒域掌控者」（橙）；战力 ≥200 → T7「末世传奇」（红），颜色与名称一致。

> 校验：`bun run tsc -b tsconfig.app.json`（exit 0）、`bun run build:client`（exit 0，built in 3.40s / 3.45s）。`chargen.ts` / `persistence.ts` / `menu/views.tsx` / `play/route.tsx`

**⑪ 角色页 / 英雄榜去掉独立 rarity（品质）标签，仅留段位徽标**
- 背景：`rarity`（普通/精锐/精英/传奇）为生成时的「先天资质」（每维属性加成 + 初始词条数），`tier` 为后天战力段位。两者语义不同，但 UI 上双双并排展示显得重复。
- 处理（用户选 A 方案）：
  - 去掉独立 rarity 标签——`menu/views.tsx: ViewSkills`（掌握技能/角色页）移除右上角 `{rarityLabel} Pill`；`ViewLeaderboard`（英雄榜）移除「稀有度」整列（表头+单元格），姓名颜色由 rarity 色改回中性 `text-zinc-100`。
  - 副本内 `play/route.tsx` 角色列表卡同样删掉 rarity 小标签（段位徽标紧随其后，已不重复）。
  - **保留**：`ViewRecruits`（花名册）与副本内招募卡的 rarity 标签——此处用于判断「兵胚值不值得招募」，仍有独立价值。
- 校验：`bun run tsc -b`（exit 0）、`bun run build:client`（exit 0，built in 3.40s）。`rarity` 字段与生成逻辑不动，仅去展示冗余，无任何数值/玩法影响。

### v1.1.2（2026-09-10 首发 · 2026-09-11 补发 · 已发布）

> 群怪战斗日志与战斗回放补全、Boss 狂暴两阶段梳理、升级天赋池扩至 60、天赋卡显示优化；补发：连升多级的天赋三选一改为队列式（xN 分组轮流选，避免重复天赋）。

**① 群怪战斗日志强化文案（修正 v1.1.1⑤ 不到位处）**
- 每只小怪的 ⚔ 交战行现在带群序标签：〔敌群 X/Y〕与【名】交战（……），一眼可辨这是一次多敌遭遇中的第几只。`ExtractionEngine.ts: fightOne`
- 遭遇/逐只/清缴三段日志重写更具废土味的描写：「为首的【名】率先扑来，其余在阴影里蠕动」「从左侧（右侧/身后/斜刺里/废墟缝隙）扑了上来」「残敌立刻补位——还有 N 个敌人围了上来」「全部放倒，此役累计损失 N 点生命（剩余 cur/max，血量 P%）——废墟暂归死寂」。

**② 战斗回放「完善体现」多敌遭遇（v1.1.1⑤ 补全）**
- 此前每只小怪各生成一条独立回放、各自展开，看不出「群战」整体。现把同一敌群（连续、groupTotal>1）的逐只回放在 UI 收拢为**单个可折叠「敌群战斗回放」**：
  - 汇总行：共 Y 只 · 总回合 · 输出 X / 承伤 Y · 全歼/阵亡；
  - 块内逐只子卡：第 X/Y 只：【名】✓击倒/✗阵亡 + 该只的六维点评/逐回合/描写；
  - 底部敌群合计行：全歼/未能全歼 · 累计输出/承伤 · 残血。
- `BattleReplayEntry` 新增可选字段 `groupIndex` / `groupTotal`，由 `buildBattleReplay` 写入；单敌战斗不受影响。`extraction/types.ts` / `ExtractionEngine.ts` / `play/route.tsx`

**③ Boss 狂暴两阶段日志/回放梳理（修正 v1.1.1⑤ 迷惑呈现）**
- 原实现把 Boss 普通形态（第一阶段）的战斗过程隐藏，只在第二阶段开始输出一条「血量跌破 50% 进入狂暴」提示，随后直接展示第二阶段交战；日志顺序极易让人误解为「Boss 被打死后才狂暴」。
- 现在 Boss 战的日志/回放按真实两阶段展开：
  - 第一阶段交战日志 + 完整回放（把 Boss 从满血压至 50% 以下）；
  - 狂暴转折提示：`👹【名】血量跌破 50%，进入狂暴——六维骤升 ×1.5，第二阶段开战！`；
  - 第二阶段交战日志 + 完整回放（狂暴形态被最终击倒）；
  - 最终击退日志改为显示「整场战斗历时 N 回合」；场景文本也同步区分两阶段回合数。
- 若 Boss 第一阶段就把玩家击倒，则只输出第一阶段交战日志与回放，不再提示狂暴。`ExtractionEngine.ts: fightOne`

**④ 升级可选天赋池 14 → 60 个（白16/绿12/蓝10/紫10/黄6/橙4/红2），并重新平衡抽取权重**
- 原 `TRAIT_POOL` 仅 14 个（绿6/蓝5/紫3），旧权重 `white:8` 在扩池后会严重偏向白档。本次按用户指定分布补齐至 **60 个**：保留原有 14 个（id/文案/数值不变），新增 46 个（白16 / 绿6 / 蓝5 / 紫7 / 黄6 / 橙4 / 红2）。
- 数值曲线贴合废土风且与品质递增：白=纯小属性（无战斗增益）；绿=小幅搜刮/暴击/气血/初始血；蓝=中等；紫=强暴击/气血；黄=传说级双增益；橙=神话级；红=禁忌/文明余烬级（如「终焉使徒」力量+8体质+4敏捷+2 / 暴击+45% / 气血+60；「文明余烬」体质+9耐力+5感知+3 / 气血+120 / 搜刮+70% / 初始血+35%）。
- `TRAIT_PICK_WEIGHT` 重写为随品质递减：`white:2.0 / green:2.0 / blue:1.6 / purple:1.2 / yellow:1.0 / orange:0.8 / red:0.6`。模拟（40 万次无放回抽 3）实测每类天赋在某次三选一出现概率：白≈6.3%、绿≈6.3%、蓝≈5.1%、紫≈3.85%、黄≈3.2%、橙≈2.6%、红≈1.95%；三选一内各品质候选位占比 ≈ 白34% / 绿25% / 蓝17% / 紫13% / 黄6% / 橙3% / 红1%。`chargen.ts: TRAIT_POOL / TRAIT_PICK_WEIGHT`
- 校验：`bun run tsc -b tsconfig.app.json`（exit 0）、`bun run build:client`（exit 0，built in 3.51s）。`AffixTierKey` 七档（白绿蓝紫黄橙红）的 `affixColor/affixLabel` 已原生覆盖，UI 自动按品质着色。

**⑤ 升级三选一天赋卡「属性加成 / 增益效果」显式分行标签（修正"看不到属性加成"的视觉混淆）**
- 每项新天赋都同时配了 `modifiers`（属性加成，如 力量+4 敏捷+2）与 `combat`（增益效果，如 暴击+12% 气血+30），与原 14 个一致；白档按设计纯属性、无增益，彩色档两者皆有。
- 原显示把属性加成与增益效果挤在同一行同色（翠绿）文本里，难以区分。现改为：候选卡（`play/route.tsx` 升级三选一）与悬浮气泡 `TraitBonusText` 均**分行 + 标签**——「属性加成：…」（翠绿）/「增益效果：…」（天蓝），候选卡并补上原先漏显示的「初始血量」增益。`play/route.tsx: TraitBonusText / 候选卡渲染`

**⑥ 连升多级的天赋三选一改为队列式（xN 分组轮流选，避免重复天赋）—— v1.1.2 补发**
- 问题：副本内一次性连升多级时，若前面升级的三选一未选，后续升级可能在同一个面板里重复出现同一天赋（旧 `pendingTraitPick` 为扁平数组，两次升级各自独立抽取，互不排除 pending 项）。
- 改法：`pendingTraitPick` 类型由 `SurvivorTrait[]` 改为 `SurvivorTrait[][]`（分组队列）。`grantSortieXp` 升级时一次性按 `levels*3` **无放回**抽取候选，拆成 `levels` 组排队；排除集合 = 已拥有 + **仍在待选队列中的词条**，故无论单次连升多级还是多次升级间隔未选，都不会再出现同一天赋。`chargen.ts: rollTraitCandidates` / `state.ts: grantSortieXp`
- UI：始终只展示当前第一组（`sets[0]`），选完该组后整组移除、下一组自动顶上；面板显示「剩余 N 组，三选一」进度；角色页与出击页两处渲染同步。`play/route.tsx: onPickTrait / 候选卡渲染 / 副本内词条卡`
- `chooseTraitPick` 签名简化为 `(survivorId, candidateIndex)`，固定操作第一组。`state.ts: chooseTraitPick`
- 校验：`bun run tsc -b tsconfig.app.json`（exit 0）、`bun run build:client`（exit 0，built in 3.33s）。

### v1.1.1（2026-09-10 · 已发布）

> 出击/副本内体验修复与增益重做，共 6 项。

**① 修复临时背包装备加成 UI 重复**
- 临时背包的装备物品行原本同时渲染 `affixes`（词条名）与 `GearBonusChips`（同一组加成），导致「金币获取 / 搜刮运势 / 气血 / 暴击率」等重复显示。改为：仅当物品**非装备**（`!it.gear`）时才渲染 `affixes`，装备统一用 `GearBonusChips` 展示，消除重复。

**② 副本中投掷物可从临时背包检索使用**
- 副本内搜到的投掷物（破片手雷 / 烟雾弹 / 闪光弹）本就在临时背包占格子，但此前没有使用入口（只能等撤离后入库才能用）。
- 临时背包为每个投掷物新增「💣 检索使用」按钮：点击即从战利品背包移出并**装入基地投掷袋**（`state.throwables`），随后即可被战斗中的快捷投掷（手雷自动引爆）与「投掷物脱离」消耗。占用格子、可检索使用，两全其美。

**③ 副本装备栏顺序对齐背包页**
- 副本「本局穿戴」原按佩戴先后顺序排列，换装会把顺序弄乱。改为统一按 `MAIN_EQUIP_SLOTS` 固定顺序渲染，与基地背包页完全一致。

**④ 临时背包显示顺序规则**
- 临时背包排序：`消耗品(药物等) → 材料 → 装备`；装备段内部按**阶级品质降序**排列。排序时保留原始下标，确保「使用/佩戴/移安全箱/丢弃」操作的仍是正确物品（修复排序后操作错位回归）。

**⑤ 战斗日志体现多名敌人（群怪）**
- 危3+ 群怪本就逐个击破、血量跨场继承，但日志只显示最后一只、且只报单场掉血，看不出「与多名敌人交战」。
- 现改为：群怪逐只播报「敌群第 X/Y 只：【名】扑了上来」，战后再补一条「敌群清缴完毕：共 Y 只，累计损失 N 点生命（剩余 cur/max）」，一眼看清真实战损。

**⑥ 肾上腺素增益重做（计时 + 六维全 +5）**
- 增益补给（原「兴奋剂」，已更名**肾上腺素**）效果由「按次备战、单场部分六维 +5/+5/+3/+2」改为**副本时间 10 分钟内六维全属性 +5** 的计时增益。
- 增益注入点统一收口到 `runEffectiveAttributes`：战斗与出击页六维显示**同一来源取数**，开启后页面六维实时 +5、战斗unit 同步 +5。
- 出击页增益补给文案（按钮副标题、状态条、buff 角标）同步改为「剩余 N 分钟 / 六维全 +5」。
- 获取链确认齐备：**产出**（副本掉落 `LOOT.stim`）、**制作**（`craft-stim` 调配肾上腺素）、**交易**（废土市场 `buyMedicine`）、**种植**（菜园「兴奋草」→ 肾上腺素）。

**⑦ 修复医疗中心 HP 血条不显示**
- 医疗中心角色卡用 `hpBar(currentHp, maxHp)` 渲染血量条，但 `hpBar` 的配色用了非法 Tailwind 类 `bg-emerald-950/300` / `bg-amber-950/300` / `bg-rose-950/300`（`/300` 不是合法透明度后缀 → 填充条无背景色 → 血条「看不见」）。改为合法高对比色 `bg-emerald-500` / `bg-amber-400` / `bg-rose-500`，血量颜色条正常显示。`views.tsx`

**⑧ 背包/装备页快捷槽分类：肾上腺素归增益槽、其余归医疗槽**
- 快捷消耗槽下拉原对非投掷槽统一列出全部药品，导致肾上腺素（stim）同时出现在「医疗」与「增益补给」两个槽。现按槽过滤：「增益补给」槽**仅可选肾上腺素（stim）**，「医疗」槽列出除 stim 外所有药品。`play/route.tsx`

**⑨ 避难所菜园每级加成文案：每级减少 10% 种植时间（实装核验通过）**
- 菜园种植时间缩短逻辑 `gardenCropDurationMs` **早已实装**（`factor = Math.max(0.3, 1 - 0.1 * gardenLevel)`，即每级 -10%、下限 30%），与你测试一致。
- 但基地设施卡的「每级：」文案由 `lootPerLevel` / `recoveryPerLevel` / `discountPerLevel` / `attrPerLevel` 拼出，菜园这些字段全 0 → 显示「暂无数值加成」，看不到该效果。
- 修复：设施 spec 新增 `plantTimeReductionPerLevel`（菜园 = 0.1），基地设施卡「每级：」文案加入「种植时间 -10%」，菜园 description 同步更新。「每级减少 10% 种植时间」现已在基地页面正确展示。`economy.ts` / `play/route.tsx`

### v1.1.0（2026-09-10 · 跨版本功能完善）

> 针对「末世行止」功能普查发现的问题，做第一批止血与正名改造。

**① 重塑天赋 → 重塑六维（修复「变相销毁角色」严重 Bug）**
- 旧实现用 `generateSurvivor()` 生成**全新 id** 后整体替换成员，导致装备（按 id 存）、状态、等级全部断裂，`activeSurvivorId` 指向失效成员。
- 新实现 `rerollBaseAttributes()`：**不重建成员对象**，只改写 `baseAttributes`（初始六维基础属性），成员 id / 装备 / 状态 / 等级 / 经验 / 词条全部保留。
- 规则：消耗 **500 废土币**，六维各在 **6~20** 重新随机；「词条加成 + 升级加点」的差值被保留并叠加回新基础值上；段位随新战力重算。
- `SurvivorProfile` 新增可选字段 `baseAttributes`；旧存档由 `ensureBaseAttributes()` 迁移补齐（以「当前属性 − 词条加成」回填）。

**② 行动点系统（v1.1.0 新增 · 抑制无限刷）**
- 新增存档字段 `actionPoints` / `actionPointsAt`：**初始 120 点（即上限）**，每 **5 分钟恢复 1 点**，满点后暂停恢复。
- 出击消耗：危1 = 6 点，每级 +1，**危7 = 12 点**（`sortieActionPointCost(dangerLevel) = 5 + danger`）。
- 手动出击（进图瞬间）与「漫游搜打撤」同样扣点，不足时直接拦截，不再能无限点击刷物资。
- 载入存档时先结算离线期间应恢复的点数（`tickActionPoints`）。
- 漫游搜打撤记录写入存档 `wanderLog`（最近 10 条），不再只存组件内存、切页即丢。

**③ 移除「世界种子」UI**
- `worldSeed` 在引擎中无任何消费点（换种子不影响区域/敌人/战利品生成），属假功能，暂时移除该设置卡片。

**④ 鉴物回收 → 装备改装（正名）**
- 该页实际是「消耗材料随机造装备」，与「回收」语义相反，改名为**装备改装**；真正的出售统一到废土市场。

**⑤ 出售统一到废土市场（支持选物品 + 按稀有度浮动价）**
- 材料：可选 ×1 / ×5 / 全部，单价 = 基础价值 ×2（与旧「10 币/件」口径一致）。
- 装备：可多选仓库装备（已穿戴的需先卸下），售价 = `value × (1 + 稀有度阶级 × 0.2)`，白 1.0× → 红 2.2×。
- 背包页的装备回收同步改用同一浮动价。

**⑥ 英雄榜补上截断**
- 文案自称「前 30 名」但代码未截断，现已 `.slice(0, 30)`（战团上限 10 人，作为兜底）。

**⑦ 重塑六维增加二次确认**
- 花费 500 币且不可逆，改为**内联二次确认框**（确认重塑 / 取消），不用 `window.confirm`（在本壳内不可靠）。

**⑨ 菜园种子系统（修复「无限免费种植」）**
- 此前菜园可无限免费种植，成熟即白拿医疗品/废土币，是个无成本收益点。
- 现在**种植必须先消耗 1 颗对应种子**；存档新增 `seeds`（作物 id → 数量）库存。
- 菜园页新增**种子商店**（×1 / ×5 购买）。定价铁律：**种子价恒低于产物市价**，差额即等待成熟的利润。
  | 作物 | 种子价 | 产物（市价） | 净赚 | 耗时 | 币/分 |
  |---|---|---|---|---|---|
  | 🌿 草药 | 7 | 止血绷带（15） | +8 | 6 分 | 1.33 |
  | 🍄 变异菌 | 12 | 抗生素（25） | +13 | 12 分 | 1.08 |
  | ⚡ 兴奋草 | 22 | 兴奋剂（40） | +18 | 14 分 | 1.29 |
  | 🌾 高能作物 | 33 | 急救箱（60） | +27 | 25 分 | 1.08 |
  | 🩸 血清藤 | 38 | 血清（70） | +32 | 20 分 | 1.60 |
  | 🧬 纳米菇 | 70 | 纳米凝胶（120） | +50 | 35 分 | 1.43 |
  | 🥫 口粮作物 | 16 | +30 废土币 | +14 | 8 分 | 1.75 |
- 新存档赠送开局种子 `{ 草药×3, 口粮×2 }`；旧存档加载时同样补发，避免菜园直接锁死。
- **未成熟就铲除会损失该颗种子**，因此铲除加了内联二次确认（未成熟时红字警告）。

**⑧ GM 调试功能（系统设置 · 需密钥）**
- 系统设置页底部新增 **GM 面板**，默认锁定，输入正确密钥后解锁（密钥 `5362895`；解锁状态记在本机 `localStorage`，可随时点「锁定 GM」退出）。
- 解锁后三个按钮：**出击者经验 +200**、**废土币 +500**、**恢复行动点 120**（一键满行动点）。
- 经验走正常升级流程：`grantSortieXp` 满经验即升级 → 发放自由属性点、触发词条三选一、状态回满。
- 行动点恢复自动夹到上限（已满则跳过并提示「已是 120/120」）。
- 所有 GM 操作都会写一条【GM】日志。

**⑩ 漫游搜打撤改为「模拟真实副本」（修复「无损白嫖」）**
- 旧实现漫游是一次性无伤结算，等于无限刷物资还不掉血，与「搜打撤」内核矛盾。
- 新实现 `simulateWanderSortie()`：**复用与手动出击完全相同的建局流程**（起始血量/护甲/弹药/已带伤势、行动点扣费），并执行「搜刮 → 遇敌 → 战斗 → 再搜刮 → 主动撤离/被拖死」的模拟循环，最后按手动出击同口径结算：
  - 战利品入库（撤离失败仅保留安全箱）；搜刮废土币入账（失败则遗失）；
  - 战斗经验入账（仅撤离成功）；掉血回写、战后伤势带回；失败按概率被夺走已穿戴装备；阵亡/超时进入濒死；
  - 行动点照常消耗（不足直接拒绝，不产生收益）。
- 智能撤退：血量 < 40% 主动冲撤离点；血量 < 35% 遇敌改潜行绕行；残血（< 30% 当前血量）**拒绝派出**，先治疗再漫游（避免白送行动点）。
- 战报写入存档 `wanderLog`（最近 10 条）与系统日志。
- 落点：`src/shared/engine/survival/wander.ts` + `src/react-app/routes/survival/menu/views.tsx`（`ViewWandering`）。
- 冒烟测试（`v110_wander_smoke.ts`，30 局抽样）：成功 26 / 阵亡 4；掉血 27 次、负伤 12 次、濒死 4 次；累计消耗行动点 261、经验 +1559、废土币 +2075。行动点不足 / 角色濒死 / 残血三种拦截均生效。

**⑪ 危3+ 副本群怪（机制化难度 · 伤势死亡螺旋）**
- 危险度 ≥ 危3 的非 boss 遭遇，进入战斗时随机生成 **1~3 个小怪成队**（`groupSize = 1 + floor(rng()*3)`），逐个击破、玩家血量**跨场继承**（前一只怪造成的伤势直接带入下一只，自然形成死亡螺旋）。
- 同队小怪从本区敌池抽取（`pickEnemy(state, rng)`），非首只随机补位；每只之间消耗 `0~15` 分钟推进时间。
- 落点：`ExtractionEngine.ts` 的 `fight()` 重写为群怪循环 → 抽出 `fightOne(state, enemy, rng)` 承载单场决斗；新增 `GROUP_MIN_DANGER = 3` / `GROUP_MAX = 3` 常量。

**⑫ BOSS 狂暴阶段（血量 <50% 时六维 ×1.5）**
- 所有危险度（危1~危7）的 boss 都有狂暴阶段：决斗分两阶段——第一阶段以基础形态开战，玩家获胜即代表 boss 已被击破 50% 以下 → 第二阶段以**六维 ×1.5** 重建敌体、玩家当前血量继承，并播报「【X】血量跌破 50%，进入狂暴」。
- 关键实现：battle-v5 共享引擎每回合经 `BattleStateCodec` 重建 unit，调用方 live 引用首回合后即脱离战斗单位，故**不在 shared 引擎挂 live 钩子**，改为副本适配器层做两阶段 duel——既不碰 battle-v5（保护修仙侧），又 100% 保证狂暴触发。
- 新增 `scaleEnemyAttributes(base, mult)` 缩放助手（六维各 `max(1, round(v*mult))`）；狂暴日志、战斗回放、胜利场景文案均已接入。
- 验证（真实引擎 200 局/项）：满装 Lv15 各 boss 狂暴 100% 触发、胜率仍 100%、残血从 82%+ 降至 69%~84%、战斗拉长到 12~14 回合（对比无狂暴 5~6 回合）；轻装角色狂暴前已阵亡（符合预期）。群怪：轻装 Lv5 连战 3× 重装暴徒 0% 胜、3× 掠夺者损 98%——真实威胁、不踩数值悬崖。

---

### v1.0.13（2026-09-09）— 出击中成员禁止遣散
- **出击中成员禁止遣散**：战团成员在副本中出击时（同卡片已定义的 `inSortie = sortieId === s.id`），其战团卡片的遣散入口改为灰态「出击中·不可遣散」并禁用，避免副本进行中误遣散正在出击的成员。逻辑与既有「濒死·不可遣散」并列。
- 实现说明：`run`（副本运行态）是 React 组件层 `useState/useRef`，不进 `SurvivalGameState` 存档，故引擎 `dismissSurvivor` 读不到「谁在副本中」，拦截点放在 UI 层（play/route.tsx 战团列表）。变量 `inSortie` 在同作用域已定义，仅需补入遣散分支判断。落点：`src/react-app/routes/survival/play/route.tsx`（`inSortie` 分支）。

- **潜行绕行成功率下调（难度调整）**：`ExtractionEngine.ts` 的 `resolveEncounter` 潜行分支基准值 `普通敌 0.72→0.30`、`Boss 0.35→0.15`（敏捷 10 基准时普通敌 30% / Boss 15%，钳制 10%~95% 不变）。效果：低/中敏捷段绕行容错被压低，Boss 几乎遇必战，逼玩家改用「投掷物脱离」（烟雾弹/闪光弹）保底。敏捷仍按 `sneakBonus=(speed-10)*0.03` 线性加成。

- **力量背包注释修正（顺手修）**：`ExtractionEngine.ts` 的 `deriveAttrEffects` 文档注释把力量背包容量误写为「每 5 点 +1 格」，实际自 v1.0.5 起代码为「白字每 1 点 = 1 格」（`runPackCapacity`，见 L155/L258 注释）。仅修正注释，不影响逻辑。

- **伤势抗性注释修正（顺手修 · 第④项）**：`recovery.ts` 的 `rollCombatInjuries` 两处抗性注释与代码不符——「意志 10 → ×1.0」实际为 `1 − 10×0.015 = 0.85`；「体质 10 → ×1.0」实际 `1 − 10×0.01 = 0.90`。已改写为真实系数（意志 10→0.85 / 20→0.70 / 30→0.55封底；体质 10→0.90 / 25→0.75 / 40→0.60封底）。纯注释，逻辑不变。

- **命中/闪避重做（独立结算 + 软曲线 + 命中地板 · 第⑤项）**：原 `DamageSystem` 用 `dodge = clamp(闪避率 − 命中率, 3, 45)`，而命中/闪避同源 SPEED 同形曲线，导致净闪避恒为负、永远 3% 地板（闪避形同虚设）。改为：① `AttributeSet` 中 `命中率 = 0.70 + curve(max(0,SPEED-10), 11, 0.28)`（软曲线渐近 0.98）、`闪避率 = 0.10 + curve(max(0,SPEED-10), 25, 0.50)`（软曲线渐近 0.60），不再硬封顶；② `DamageSystem` 改为独立结算 `有效命中 = max(0.20, 命中率 × (1 − 闪避率))`，单次掷骰定命中，未命中再按原比例拆「闪避/落空」。落点：`battle-v5/units/AttributeSet.ts`、`battle-v5/systems/DamageSystem.ts`；同步更新 `AttributeSet.test.ts` 与 `ActionExecutionSystemIntegration.test.ts` 断言。

### v1.0.12（2026-09-09）— 霸主唯一性修复 + 出击页 UI 重构

本版累计五批改动：① 修复副本反复转移刷 boss 的漏洞；② 把出击页头部与面板彻底重排成手机优先的紧凑布局；③ 整合场景叙事与系统消息日志；④ 合并药物与增益补给区；⑤ 当前区域外框随深度 1~7 从白变红。

**核心修复（1 项）**

- **霸主唯一性：取消 boss 参与转移伏击**：之前身处或进入霸主区时，`moveToNode` / `advanceBranch` 的「转移伏击」直接 `pickEnemy(state, rng)` 用整张 `state.zone.enemies`，会把 `boss:true` 的霸主也抽出来，造成「反复转移刷 boss」。两处伏击改为 `ambushPool = state.zone.enemies.filter(e => !e.boss)`，池为空则跳过伏击；霸主仍只由 `rollEncounter` 在「霸主区第三次搜刮」登场，保持唯一性。落点：`ExtractionEngine.ts`。

**出击页 UI 重构（4 批）**

- **场景叙事 + 系统消息日志整合为更紧凑单一面板**：场景 `pre` 加 `line-clamp-3`（限高 3 行，全文仍存于日志），日志区 `max-h-[150px]→[112px]`、字号 10→9px、内边距收紧，两块标签压缩为 9px 小字。
- **头部 sticky 面板重排（手机优先）**：Row1 ⏱对局剩余 + 💰携带估值同处一行；Row2 位置 / 撤离点 / 威胁紧凑成行；Row3 ❤生命 与 ⬆Lv经验 双栏同尺寸进度条；Row4 🛡护甲 / 🔫弹药 / 🎒负重 / 📦安全箱 四列网格。6 维属性、增益 buff、自由属性点、词条三选一全部并入主面板，删除原独立蓝色卡片，主面板内边距 `p-4→p-3`，整体高度比之前矮约 1/3。
- **💊使用药物恢复 与 🧪增益补给 合并为 1 个 UI 区域**：原两个独立卡片合并为同一卡片，内部 `grid-cols-1 sm:grid-cols-2`（手机纵向、宽屏左右双栏），内容原样保留；任一存在即显示。
- **当前区域外框深度配色**：当前所在区域框由固定绿色改为随 `depth` 1~7 从白 `#cbd5e1` 渐变到红 `#f87171`（复用装备七阶 `tierColor`），区域名文字同步着色，越深越凶一目了然。

> 所有改动纯前端，`build:client` 验证通过；旧档无缝衔接。

**补充修复（2026-09-09 后续 · 同版本合并）**

- **手机端 Row 4 错位对齐**：护甲 / 弹药 / 负重 / 安全箱 四列由横排 `flex items-end justify-between`（窄列撑爆折行）改为纵向 `flex flex-col items-start`（标签在上、数值在下），四列严格对齐。
- **角色页加点气血逻辑回归（修正上一版误改）**：上一版曾为修「角色页加点不加血」而**删除** `recomputeMaxHpFor` 的 `if (newMax <= st.maxHp) return` 早返守卫、改为无条件按 derived 写回——这是**错误修复**，会压垮 `freshStatus` 给新人的 600「新手保护」缓冲（低体质新人 derived≈538 但 `st.maxHp=600`，加 1 体质 derived 仍 <600），造成副本中角色页加体质「血量倒扣」（见下第 5 项）。**早返守卫必须保留**，它是新人缓冲的护盾。
- **连升 N 级只 1 组三选一（bug）**：`grantSortieXp` 旧逻辑「已有候选则不覆盖」，导致一次性连升多级的自由点累计正确、但词条三选一只有 1 组。改为每升 1 级追加 3 候选累积；`chooseTraitPick` 签名改双下标 `(state, id, pickSetIndex, candidateIndex)`，选中后整组 3 个一次性移除；UI 按 3 个一组 chunk 渲染多个「三选一」区块，多组时标题显示 `(1/3, 三选一)` 进度。
- **「自由点 ×N」徽标合并**：出击页 Row 3 + 角色页等级行的独立 `自由点 ×N` 徽标删除，统一合并到「分配自由属性点 ×N」标签。

- **副本中角色页加体质「血量倒扣」回归修复（第 5 项 · 当前）**：根因同上——删除早返守卫后，`recomputeMaxHpFor` 把 `st.maxHp` 从 600 无条件压到 derived(≈558)；`syncProfileMaxHpDeltaToRun` 仅当 `delta>0` 才同步当前血，于是副本 `hp.max` 降到 558 而 `hp.current` 仍 600，UI 显示「当前 > 上限」的怪象（用户截图 623/581 与约 42 差额吻合）。修复：**恢复早返守卫**，仅当 derived 严格大于 `st.maxHp` 才提升上限，新人缓冲被保留、随加点自然自愈。落点：`state.ts:recomputeMaxHpFor`（含回归注释 + 误导性旧注释清理）。


### v1.0.11（2026-09-09）— 濒死与等级门槛治理 + UI 紧凑化 + 副本内容贴合

本版累计三轮改动，核心目标：① 把「难度」交还给等级与装备成长（低等级禁入高危区、濒死禁出击 / 禁遣散）；② 出击内 UI 进一步紧凑、与末日废土叙事统一；③ 各大副本 16 区名贴合主题。

**第一轮（3 项）**

- **濒死角色禁止再次出击**：副本结算横幅「再次出击」按钮 `disabled={activeDying}`（濒死 = `survivorStatus.dyingUntil` 未过期），`start()` 顶部加濒死守卫；出击前选择页「出击 ▶」按钮同步 `disabled={selLocked || activeDying}`。根因：之前濒死角色点再次出击会带伤复活在副本入口。
- **副本等级门槛**：新增 `DANGER_LEVEL_REQ`（危1→不限 / 危2→Lv3 / 危3→Lv5 / 危4→Lv8 / 危5→Lv10 / 危6→Lv12 / 危7→Lv15）。选择页每个副本按钮按 `active.level` 判定 `disabled` 并显示「需 Lv.N」徽标；`start()` 与「出击 ▶」双保险拦截。1 级角色彻底进不了危7。
- **副本难度提升（靠等级门槛，而非盲目放大敌人数值）**：实测战斗引擎存在「数值悬崖」——敌人基础六维仅放大 ~5% 即让通关胜率从惨胜跌到 0%（疑似跨过秒杀阈值）。故难度主要由「等级门槛 + 区域危险度（装备爆率）+ 深度词缀（遇怪）」三层间接控制；另埋 `ENEMY_DANGER_SCALE` 钩子（content.ts / ExtractionEngine.fight 套用），当前全 1.0（no-op），留作日后充分实测后再调。

**第二轮（6 项）**

- **热更新文案 → 废土风**：`AppVersionNotifier.tsx` 文案「天地法则已有更新…」改为「通讯台重新收到总部短波——幸存者档案已更新版本。刷新页面即可重返废土，旧档无缝衔接。」；按钮「刷新进入新版本」→「重新接入新版图」。
- **修复 boss 深7 区域可直接撤离 bug**：`doExtract()` 逻辑层加 `if (!s.atExtract)` 守卫（之前仅有 UI 层 `disabled` 拦截，渲染节流 / 状态时序可能绕过）。未到撤离点（未击败本区霸主）点撤离会被拦截并写日志 `⛔ 此处尚未触发撤离信号——继续搜刮或击败本区霸主。`
- **角色属性 UI 整合进次级 sticky + 删除原提示文字**：出击内角色属性 / 增益 / 伤势区块改为 `sticky top-[152px]` 次级固定。
- **场景区域再压缩 1/3 + 系统消息日志减半**：场景叙事字号 13→10px、行距收紧；日志区 `max-h-[300px]`→`[150px]`、字号同步缩 1/3。
- **搜刮 3 次提示紧贴当前区域名**：原独立黄字「此地已被搜刮干净，请前往下一区域。」删除，改为在「📍 区域名」后追加固态「（已搜尽）」（仅 `searchLeft<=0` 时）。
- **各大副本 16 区名独立化（核心）**：`DangerZone` 加 `subZones?: ZoneBranch[]` 字段，给 7 张地图各写一份贴合主题的 16 区名；`generateZoneGraph(theme, rng)` 改用 `theme.subZones ?? ZONE_POOL`。根因：之前 7 张图共用全局 `ZONE_POOL` 的同一份 16 区名，造成「走到哪个副本 16 区名都雷同」。

**第三轮（2 项）**

- **角色属性 UI 合并进经验条卡片、与经验条共处同一固定区域**：把角色属性 / 增益 / 伤势区块从独立的次级 sticky 移入经验条卡片（经验进度条之后、`自由属性点`之前），随经验条一同固定滑动；删除「角色属性（红字=伤势削减）」文字标签。
- **濒死成员禁止遣散**：`dismissSurvivor` 引擎层加濒死守卫（成员 `survivorStatus.dyingUntil` 未过期则拒绝并写日志 `【遣散】X 正处于濒死状态，无法遣散，请先救治。`）；战团成员卡片的遣散按钮在濒死时显示灰态「濒死·不可遣散」，避免带伤成员被悄悄移除。


### v1.0.10（2026-09-08）— v1.0.9 补充 + 两轮本版补充

boss 战利品、撤离成功 UI 位置、装备加成统计、基地文案、重置档案五项补充；本版第一轮「重生者姓名弹窗 / 主角词条精简 / 装备爆率查表 / 7 图难度对齐」四项补充；本版第二轮「区域后缀改为深度 1~7（与装备爆率解耦）/ 出击中重置存档」两项补充：

- **取消 boss 尸体再搜刮，boss 战利品击败自动获得**：击败霸主时一次性发放完「霸主战利品（红阶）+ 霸主遗物（橙阶）」两件，并 `state.corpse = undefined` 不创建 boss 尸体，**避免「放弃撤离→再搜一次 boss」额外刷装备**。原 `lootCorpse` 中的 `wasBoss` 块同步移除（已在此处合并发放）。
- **撤离成功 UI 移至系统消息日志下方**：副本面板内的撤离成功 / 失败 / 超时结算卡片，从原来的面板底部移至系统消息日志下方（区域②之后、③之前），**避免战斗日志长时把结算推到底看不到**。
- **背包「已穿戴装备属性增益」补齐其他词条**：除原有六维属性外，新增「气血 +N / 暴击 +N% / 搜刮运势 +N% / 经验加成 +N% / 金币加成 +N%」5 类 CombatBonus 词条聚合统计（数据来源：`gear.combat.{hpBonus,critBonus,lootLuck,xpBonus,coinBonus}`），非出击态走 `aggregateGearCombat(state, id)` 单一来源。出击内临时换装时实时同步。
- **基地页面医疗站每级文案修正**：恢复速率加成由 `+0.2%` 改为 `+${(recoveryPerLevel*100).toFixed(0)}%`（即 `+20%/级`，与训练场「+X/级」格式一致；当前 Lv5 = +100% 翻倍恢复未变）。
- **重置存档玩家代号兜底修正**：去掉「'幸存者'」硬编码兜底。优先使用 `state.playerCodename` → 主角 `survivors[0]?.name` → 都为空则弹窗提示「未找到玩家代号，请先登录或建立档案再重置」并阻止重建，避免老存档/损坏存档下主角被任意命名为「幸存者」。

**本轮补充（4 项）**

- **重置存档改为弹窗输入「重生者姓名」**：老存档 / 损坏存档里常常取不到玩家代号，重建后主角被命名为「幸存者」。现在点「重置存档」直接弹出输入框（默认预填 `playerCodename` → 主角名，取不到则留空由玩家自填，1~12 字，回车确认 / Esc 取消），确认后以该姓名重建主角。基地页与系统设置页两处入口统一走同一组件 `ResetSaveDialog`。
- **创建主角仅保留「末世主角」词条**：移除主角创建时的附加词条**退役兵 / 战地医护**（`makeProtagonist` 现只挂 `PROTAGONIST_TRAIT`）。旧存档在 `loadGame` 时通过 `DEPRECATED_PROTAGONIST_TRAIT_IDS` 从主角身上剥离这两条（已计入的六维保留，不做倒扣）；普通幸存者随机到这两条词条不受影响。
- **副本装备爆率改为按危险度查表（小怪非必爆、BOSS 必爆）**：
  - 新增 `MOB_GEAR_QUALITY_WEIGHTS` / `BOSS_GEAR_QUALITY_WEIGHTS` 两张**品质权重表**（危1~危7 × 白/绿/蓝/紫/黄/橙/红），`rollGearTier` 查表抽品质，替代原先「危险度越高越向高阶偏移」的连续公式；
  - **小怪不是必定爆装备**：新增 `mobGearDropChance(danger, luck)` = `20% + 4%×(危-1)`（危1 20% → 危7 44%），受搜刮运势线性放大、上限 90%。搜索翻箱与「搜刮普通敌尸」都先过这个判定；
  - **BOSS 必定爆装备**：霸主被击倒时必定发放「霸主战利品 + 霸主遗物」两件，品质走 BOSS 表（不再叠加保底阶，避免扭曲给定分布）；
  - 上述均为**基础概率权重**，实际仍受感知 / 搜刮运势（`luckBias`）向高阶拉动：`weight ×= (1 + gamma × tier)`。

  | 难度 | 白 | 绿 | 蓝 | 紫 | 黄 | 橙 | 红 |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | 危1 小怪 | 70% | 22% | 7% | 1% | 0 | 0 | 0 |
  | 危2 小怪 | 58% | 28% | 11% | 2.5% | 0.5% | 0 | 0 |
  | 危3 小怪 | 45% | 30% | 18% | 5.5% | 1% | 0.5% | 0 |
  | 危4 小怪 | 32% | 28% | 24% | 11% | 3.5% | 1.5% | 0 |
  | 危5 小怪 | 20% | 24% | 26% | 18% | 8% | 3.5% | 0.5% |
  | 危6 小怪 | 10% | 18% | 24% | 24% | 14% | 8% | 2% |
  | 危7 小怪 | 4% | 10% | 18% | 26% | 20% | 16% | 6% |
  | 危1 BOSS | 40% | 38% | 18% | 4% | 0 | 0 | 0 |
  | 危2 BOSS | 30% | 35% | 24% | 9% | 2% | 0 | 0 |
  | 危3 BOSS | 20% | 28% | 30% | 16% | 5% | 1% | 0 |
  | 危4 BOSS | 12% | 22% | 28% | 24% | 10% | 4% | 0 |
  | 危5 BOSS | 6% | 14% | 24% | 28% | 16% | 10% | 2% |
  | 危6 BOSS | 3% | 8% | 18% | 26% | 22% | 16% | 7% |
  | 危7 BOSS | 1% | 4% | 12% | 22% | 24% | 24% | 13% |

- **7 张地图难度依次对齐 危1~危7**：`DANGER_ZONES` 危险度由 `1,2,3,3,4,5,6` 改为 `1,2,3,4,5,6,7`（废弃公寓 危1 → 核爆禁区 危7）。同时图内 16 区的危险度改为**与本图难度绑定**：入口恒为 危1、最深处 = 本图危险度（此前固定 1~6，与本图无关），因此「越深越危险」与「图与图之间有梯度」同时成立；UI 危险度标签补齐 `危7·绝境`。

> ⚠️ 该项的「图内危险度随深度绑定」写法已于**本版第二轮修订**（见下），改为「全图恒定 危N + 图内深度 1~7」，原因是那样会导致废弃公寓这类 危1 图的所有区域后缀全是「危1」，玩家无法判断区域深度。

**本版第二轮补充（2 项）**

- **区域后缀改为「深度 1~7」，与危1~危7 彻底解耦**

  原先（含本版第一轮的写法）把「区域后缀的数字」与「装备爆率档位」混为一谈：图内危险度由深度推导，于是 危1 的废弃公寓全图 16 区后缀全是「危1」，看不出深浅；而深度信息又反过来污染了装备爆率。

  现在两者彻底分开：

  | 概念 | 取值 | 来源 | 影响 |
  | --- | --- | --- | --- |
  | **深度**（区域后缀数字） | 1~7 | 节点距起点的层距，归一化 `normalizeDepth` | **遇怪难度 / 遇怪概率**：威胁查表 `threatEncounterChance(深度, 威胁档)`、敌人词缀强度 `rollEnemyAffixes`、转移伏击率、救援概率、击杀经验 |
  | **危险度**（本图 危N） | 1~7 | `theme.dangerLevel`，**全图恒定** | **装备基础爆率**：`mobGearDropChance` / 小怪·BOSS 品质权重表 |

  - `ZoneNode.depth` 由「原始层距 0..max」改为归一化 1..7；`ZoneNode.danger` 由「随深度递增」改为本图难度常量；
  - `threatEncounterChance(depth, tier)` 入参由 0..6 改为 1..7（内部 `depth-1` 取行），使查表档位与后缀展示的深度一一对应；
  - 引擎新增 `zoneDepth(state)`，凡「遇怪难度 / 遇怪概率」相关（敌人词缀、伏击率 `0.05×深度`、救援 `0.1+0.04×深度`、击杀经验 `12+8×深度`）全部改用它；装备爆率仍只认 `state.zone.dangerLevel`（本图危N）；
  - UI：相邻区域按钮后缀 `危N` → `深N`；当前区域徽标显示「危N · 深度M」；出击 HUD 新增「深度 M」徽标；撤离点列表显示 `区域名 深M`；区域图标题改为「数字=本区深度，越深遇敌越凶」；转移日志补「深度 M」。
  - 效果举例：废弃公寓（危1）图内照样分 深度1..深度7，越深越容易撞上硬茬，但全图爆率恒走 危1 那一档。

- **出击状态下重置存档：重生主角不再滞留副本、状态全满**

  原先在副本中（出击中）点「重置存档」，只重建了档案，进行中的对局（`run` / `clearRun`）没被清掉；而重生主角 id 为 `protagonist-{姓名}`，与旧主角同名时完全相同 → 角色页判定其「副本中」，生命条直接读副本内当前血量，于是表现为「重生的主角还在出击状态、血量不满」。

  - 新增 Hub 级统一入口 `resetGame(name)`：**先退出出击**（`runRef=null` / `setRun(null)` / `writtenRef=false` / `clearRun(user)`）→ `clearSave()` → 以输入的「重生者姓名」`createProtagonistGame` 重建主角（初始状态由 `freshStatus` 生成，满血、无伤势）；
  - 基地页「重置存档」与「末世行止·系统设置」两处入口统一走它（`BasePanel.onResetGame` / `MenuDrawerProps.onResetGame` → `ViewProps.onResetGame`），设置页在缺省注入时退化为原行为。

### v1.0.9（2026-09-08）

稳定性与体验修复，含此前累积的三项改动：

- **升级加点偶而不加血（bug 修复）**
  - 根因：`recomputeMaxHpFor` 计算新最大生命时只计入词条气血，漏算已穿戴装备的 flat `combat.hpBonus`；当角色穿戴了 +20 以上气血装备时，`newMax` 被低估 → `if (newMax <= st.maxHp) return` 提前返回，把体质点 +20 吞掉（是否触发取决于是否穿了气血装备，故「偶然出现」）。
  - 修复：让 `recomputeMaxHpFor` 与 `recomputeMaxHpIncludingGear` 同口径（词条气血 + 装备气血），加点恒增最大生命、满血时当前生命同步增加。
- **疲劳 debuff 确认已实装**
  - 触发：本局在线时长超过 `耐力 × 1.5` 分钟后，每推进一次时间按 `5% × 超时分钟数`（上限 60%）判定疲惫。
  - 效果（经 `runEffectiveAttributes` 真实作用到副本六维 / 最大生命）：全六维 `-1/4`（`INJURY_ATTR_PENALTY.fatigue`）+ 恢复速度 `-10%`（`INJURY_PENALTY.fatigue=0.1`）。短局 / 提前撤离不会触发，故此前「未见」。
- **转移区域仍可能遇敌（调整）**
  - 即使前往的区域已搜刮清理结束，转移途中仍按「区域危险等级 + 对局已推进时长（剩余时间越少越高）」保底残余威胁概率遇敌；危险等级越高、拖得越久，遇敌概率越大。
- **重置存档二次确认 + 重生同名主角（bug 修复）**
  - 基地页与系统设置页的「重置存档」改为应用内联「确认 / 取消」二次提示，规避原生 `window.confirm` 在部分环境被禁用导致「点了没反应」的问题。
  - 重置后不再清空为空白避难所，而是重新生成一名以玩家代号命名的主角（代号取自存档 `playerCodename`，回退到当前主角名 / 默认「幸存者」），避免「重置后角色全没了」。

### v1.0.8（2026-09-08）

出击体验与恢复速率调整、潜行绕行重做：

- **耐力续航时限缩短**
  - 耐力属性效果①「续航时限」由 `耐力 × 2 分钟` 调整为 `耐力 × 1.5 分钟`（超出后判定疲惫）；效果②「耐力 × 3 气血」保持不变。
- **潜行绕行耗时改为受敏捷缩放 + UI 文案更正**
  - 潜行绕行基础耗时固定 3 分钟（180 秒），**改为受敏捷 `timeScale` 缩放**（敏捷越高耗时越短），制造「绕行更耗时、逃跑流不划算」的紧迫感。
  - 选项 UI 文案由「固定耗时 3 分钟，可能暴露」更正为「耗费大量时间潜行通过，可能暴露」。
- **恢复速率公式重做（单一来源，废止魔法数）**
  - 移除临时整体缩放 `REGEN_SCALE=0.1` 与 `regenPerMinute` 中硬编码的 `1 + 0.5×医疗站等级`，避免后续调参踩魔法数。
  - 医疗站恢复**仅走 `computeShelterBonuses` 的 `recoveryBonus` 单一来源**：`recoveryPerLevel 0.5 → 0.2`，即每级 +20%，**5 级医疗站 = +100%（×2，翻倍恢复）**。
  - 基础公式重标定：每点最大生命贡献 `0.005`（0.5%/分）；每点体质贡献 `0.4`（叠加经最大生命间接 `20×0.005=0.1` 后约 `+0.5/min`）。
  - 效果：角色 yemo 恢复速率由实测 ~417/min 降至 **~48/min**（医疗站 Lv5 ×2），回血不再是秒回。
- **回归验证：体质加点正确增加最大生命**
  - 引擎函数测试覆盖「出击状态内外」：体质 +1 → 档案与副本最大生命均 +20、满血时当前生命同步 +20；未发现 bug（v1.0.6 加点不掉血修复在 v1.0.8 仍有效）。

### v1.0.7（2026-09-08）

出击血量结算、副本换装生命、遣散确认与潜行绕行体验修复：

- **进出击当前血不再异常下降（修复 yemo 进图 1935→1415 掉血）**
  - `start()` 末尾当前血封顶目标由「剥离装备 HP 的 gear-less 值」改为副本真实最大血（基础 + 特质 + 驻防 + 装备气血），带装备成员进图不再凭空掉血；其他不穿戴此类装备成员同样不再掉血。
- **副本内换装（含体质 / 耐力装备）正确改变最大生命**
  - `equippedHpBonus` 现计入装备六维 `体质×20 + 耐力×3 + 气血词条`；出击前装备气血在进图时完整剥离，换装后按「当前实穿装备」重新累加。
  - 换装只改最大血、当前血不变（`recomputeRunMaxHp`：`current = min(max, current)`），反复穿脱无法刷血。
- **遣散战团成员二次确认（防误触）**
  - 改为应用内联「确认 / 取消」，规避原生 `window.confirm` 在部分运行环境被禁用导致「按了没反应」的问题；主角在引擎层仍硬拦截不可遣散。
- **潜行绕行固定耗时 3 分钟**
  - `ACTION_COST.sneak` 由 35 秒（受敏捷系数打折后约 25 秒）改为固定 180 秒，不再受敏捷 timeScale 缩放，制造「绕行更耗时、逃跑流不划算」的紧迫感；UI 文案同步标注「固定耗时 3 分钟」。

### v1.0.6（2026-09-08）

废土核心循环重做与换皮收尾（含首批改动 + 本轮补充）：

**首批改动**

- **换皮收尾：错误页 / 首屏加载页**
  - 错误页文案「万界道友」系列 → 「废土异常 / 废土航向偏移」；首页与 `/survival*` 路由共搜打撤「末世协议 / 系统搜打撤·加载中」加载页，不再出现「万界道友」。
- **背包装备栏·「已穿戴属性增益」统计**
  - 出击页换装后实时刷新；副本内换装同步到统计区；临时背包与背包装备库装备词条显示统一。
- **出战时驻防增益：当前/最大血量一并提升**
  - 「当前」与「最大」血量增加同样的驻防气血，修复「切另一成员出击后进图不吃全属性带来的血量增益」。
- **副本内「驻防·全属性」单独 buff 区**
  - 出击进副本后角色属性区显示「🏰 驻防·全属性+N」emerald 徽标（每属性 +N 取最小值），势力加成走 🤝 徽标。
- **底部「驻防加成」卡 → 六维汇总**
  - 由避难所设施与三势力提供的六维加成按 体质/力量/感知/耐力/敏捷/意志 顺序汇总显示。
- **设施/势力升级费用 ×5**
- **医疗站恢复速率** 0.5%/级 → 5%/级（Lv5 = 25%）。
- **训练场** 削弱撤销 → 六维全属性 +1/级。
- **势力声望效果调整**：铁壁 力量+1·体质+1；银手 意志+1·耐力+1；自由侦察兵 敏捷+1·感知+1。
- **加点 bug**：切换角色升级体质不掉血（修复 `recomputeMaxHpFor` 与 `recomputeMaxHpIncludingGear` 口径不一致）。

**本轮补充**

- **README 版本与更新日志** 同步到 v1.0.6。
- **杂散文件清理**：根目录误产物 `0`（0 字节空文件）随提交清理。
- **战团成员切换限制**：任一成员出击中时，其他成员「选为出击」按钮置灰禁用（防止双出击）。
- **副本内换装同步最大生命**：临时背包装备有「气血」时，副本最大值按 `effectiveRunMaxHp` 即时重算。
- **击败霸主后撤离点激活**：击败区域 boss 后原地（在霸主区域）可直接撤离；`moveToNode` / `advanceBranch` 进入已通关霸主区也直接设置 `atExtract`。
- **副本 `startMaxHp` 双倍装备气血修复**：`status.maxHp` 已含装备 hpBonus，副本 `createRun` 内部又加 `equippedHpBonus(state.equipped)` 造成装备 HP 重复计入。`start()` 现从 `baseMax` 扣除 `aggregateGearCombat(...).hpBonus` 后再加驻防 HP，副本 maxHp 与出击前一致。

### v1.0.5（2026-09-07）

出击与撤离体验优化 + 一批 bug 修复（含此前大地图重构与本次 2 项改动）：

- **系统消息日志自动定位最新**
  - 出击过程中日志出现新内容时，自动滚动到最新处，不再需要手动下滑查看。
- **撤离交互重做**
  - 主行动「前往撤离点」更名为「撤离」；移除区域图下方「抄近路直奔最近撤离点」按钮；
  - 撤离仅可在**身处撤离点区域**时进行：处于撤离点区域时按钮高亮（绿），否则灰色不可点；
  - 「本局撤离点」标注各区域危险等级，如「医院侧楼 危4、深井竖坑 危5」，便于规划路线。
- **非撤离点无法撤离（修复）**
  - 撤离点显形后，原仍可在非撤离点区域直接撤离的问题已修复：必须先经区域图抵达撤离点区域，再确认撤离。
- **霸主区搜刮逻辑（修复）**
  - 霸主区三次搜刮不再每次都遭遇霸主：仅最后一次搜刮触发霸主战，前两次为获得物资或概率遭遇普通 / 精英敌人。
- **佩戴装备气血同步（修复）**
  - 背包内为角色佩戴 / 卸下装备后，气血上限与当前气血实时同步至角色页（按六维 + 词条 + 装备加成重算）。
- **出击选图词条精简（修复）**
  - 选择大地图时出击者下方词条仅显示搜刮加成 / 经验加成 / 金币加成，移除气血上限、暴击、初始血量及「由 createCombatUnitFromCultivator 构建」字样。
- **背包排版（修复）**
  - 材料区、装备制造区移至装备库下方（顺序：装备栏 → 装备库 → 材料 → 装备制造）。
- **出击状态栏滑动固定（优化）**
  - 经验值区域及以上的状态信息设为滑动固定，上下滚动时人物状态始终可见。
- **末日行止·战术手册（修复）**
  - 主动技区改名为「六维基础属性」并配绿色进度条；被动技战斗词条补全六维属性加成的具体数值。

### v1.0.4（2026-09-07）

在 v1.0.3 基础上的修复与补全（含此前 3 项 + 本次 2 项，共 5 类）：

- **出击实时经验（修复）**
  - 修复「打怪不获取经验」：根因为 `doEncounter` 中 `xpBefore` 在 `resolveEncounter` **之后**才读取，导致经验增量恒为 0、`grantSortieXp` 从未触发。已将 `xpBefore` 提前到战斗前捕获，击杀经验**实时结算**写入角色档案。
- **夹板绷带（新增医疗品）**
  - 废土市场新增 **夹板绷带**（50 废土币），恢复量同止血绷带（12% 生命 + 20 点），并**额外清除骨折 debuff**；
  - 可由 **1 个止血绷带 + 2 个废金属** 制作（基地「医疗·制作台」与出击「临时制作台」通用）。
- **出击页「临时制作台」（新增）**
  - 在出击临时背包区域新增合成面板，可消耗**本局背包内搜到的材料**合成「医疗·制作台」里的物品（绷带/兴奋剂/营养剂/血清/纳米凝胶/夹板绷带）；
  - 需求物资在临时背包内实时检索并显示持有量，材料齐全方可合成，产出直接进入本局背包、可就地使用。
- **废土币显示（修正）**
  - 临时背包内废土币原本显示「废土币 ×1 = 25 废土币价值」，现已改为「废土币 ×25 = 25 废土币价值」，与实际币数一致（入库折算数值不变）。
- **装备库分类筛选（修复）**
  - 背包装备库按槽位（主武器 / 副武器 / 头部 / 躯干护甲 / 腿部 / 饰品）筛选为单选：点「副武器」仅显示副武器，不再混入主武器。
- **废土币改版（重做）**
  - 出击搜索掉落的「废土币」不再进入战局背包（不占格、不算材料），改为本局直接钱财 `carriedCredits`，在临时背包内独立显示；
  - 因价值已 1:1 调整，单次掉落由原本「1 个废土币」变为「25 个废土币」（`LOOT.credits` 的 `qty=25`）；
  - 撤离成功后 `carriedCredits` 以 **1:1** 折算入右上角基地废土币金额；阵亡 / 超时不结算（随战局背包遗失）；
  - 出击「临时制作台」合成药品时，所需废土币从本局 `carriedCredits` 中扣除；基地「医疗·制作台」仍从右上角基地废土币结算。
- **出击页日志整合（优化）**
  - 「系统消息日志」由原底部独立区块上移至「场景显示区域」下方，并整合进同一面板（场景在上、日志在下），阅读更连贯。
- **装备库排序与批量卖出（修复）**
  - 背包装备库列表改为按 **阶位（tier）从高到低降序** 显示，高阶装备置顶；
  - 修复批量卖出勾选异常：点击单件装备卡顿时无法取消、勾选会连带选中属性完全相同的其他装备、以及偶发复制/假显示的问题（根因为列表使用数组下标 `index` 作为 React key，导致勾选态错位；改为以装备稳定 `uid` 作为 key 与选中依据）。
- **撤离结算折算（修复）**
  - 修正「撤离后金额暴涨」：`bankLoot` 此前会把战利品估值（`value×qty`）一并累加进基地废土币金额，导致除废土币外的材料估值也变成钱。
  - 现改为：仅搜刮到的 **废土币（`carriedCredits`）以 1:1 折算入金额**；其余物资（材料/装备/药品等）仅作为 **估值** 入库，计入基地库存，**不折算为废土币金额**，需经「贩卖」后才转化为金额。
- **金币获取加成直抵废土币（优化）**
  - 拾荒嗅觉 / 装备「金币获取 +n%」词条现在**直接体现在搜刮到的废土币数量**上：无加成角色每轮搜得 25 废土币，金币加成 100% 的角色则搜得 50 废土币；
  - 加放在 `grantLoot` 发放货币时按 `(基础数量 × (1 + 加成))` 实时结算，并随当前穿戴（含副本内换装）动态计算；撤离结算文案会提示本局额外获得的加成部分，但不重复折算金额。

### v1.0.3（2026-09-07）

体验打磨与一批修复（相对 v1.0.2）：

- **出击实时经验与升级同步**
  - 出击战斗后经验**实时结算**并写入角色档案（不再等撤离结束）；
  - 出击途中升级 → **状态回复全满 + 伤势清除**，并弹出**词条三选一**提示、即时生效；
  - 能力加点与升级词条三选一在**角色页 / 出击页均可操作并双向同步**。
- **副本内换装规则（修正）**
  - 佩戴带「气血」装备 → 仅提升最大血量；脱下 → 仅降低最大血量；当前血量按规则夹取（满血减最大则当前同减、满血加最大则当前不变、非满血不受影响）；
  - 加体质点 → 最大血量 + 当前血量**一起提升**；
  - 此前「战斗锚点把装备/词条自带气血抵消」的 bug 已修复，副本最大血量正确计入装备与词条加成；
  - 撤离成功后，副本内换上的装备（含掉落装备）**同步回角色装备栏**。
- **六维显示修正**
  - 词条带来的六维属性算作**基础数值**（白色），绿字仅显示装备加成；未穿戴装备时不再凭空出现绿字。
- **弹药平衡**
  - 下调出击过程中「搜刮翻出弹药箱」的概率，减缓弹药获取节奏。
- **其他**
  - 撤离结算回写改用持久 `baseMaxHp`，避免临时驻防加成泄漏进角色档案；
  - 经验改为战斗中实时结算，撤离不再重复入账（仅保留拾荒嗅觉金币加成）。

### v1.0.2

转移遇敌、boss 高阶掉落、全部位掉落与投掷物/增益、暗色主题与词条重做、更名全境求生、市场确认与批量购买。

### v1.0.1

装备回收/分类、穿戴独占、六维加成显示、副本用药、经验升级与词条三选一修复。

### v1.0.0

末世搜打撤首发版本。

## 开源协议

本项目采用 [GNU General Public License v3.0](LICENSE) 协议开源。
